{
  "name": "puente-seasidehouse",
  "private": true,
  "scripts": {
    "optimize": "node scripts/optimize-images.js",
    "build": "npm run optimize && npx @11ty/eleventy",
    "dev": "npx @11ty/eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "sharp": "^0.33.0"
  }
}
